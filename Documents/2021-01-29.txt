//////////////////////Date:2021-01-22//////////////////////////
1)angular is framework to buid html,css and javaascript/typescript.
-It provides proper structure of code
-Easy to maintain complex project
-auto testable.
-use to make client side application.

2)Architecture:
+front-end
	-HTML
	-CSS
	-Angular
	-Typescript
+Back-end
	-APIs
	-Databases

3)download angular- ng -g @angular/cli
+create project- ng new proj_name
+compile project:- ng serve

4)Basic Structure of angular project:

5)HMR(Hot module reloading):automatic reload when change in code

6)versions of Angular:
-AngularJS :2010
-Angular 1
-Angular 2
-Angular 4
-Now its,just Angular	


//////////////////////Date:2021-01-25//////////////////////////
1)Typescript:
-superset of javascript
-strong typing
-object-oriented feature
-compile time error
-great tooling

2)typescript transpile into javascript so browser can understand.

3)variable declare in typescript:
-in ES5 use var for declare variable.
-in ES6 use let.

4)enum in type script:
-easy to use enum in ts then js.

5)type_assertion:
-when not declare type and after defining doing cast type it is known as type_assertion.

6)arrow_function:
-let doLog=()=>{}
-not need function keyword.

7)interface:
-first latter should be capital of interface name.
-when pass interface to method ,it will cohesion.
-solution is class.

8)class:
-collects variable and methods.


//////////////////////Date:2021-01-28//////////////////////////
9)constructor:
-it can't have multiple constructor.
-so,if we want to optional argument then put "?" beside arg name.
ex.constructor(x?:number,y?:number){}

10)access modifiers:public,private,protected
-public is default.

11)access modifier in constructor parameters
-Ex.constructor(private x?:number,private y?:number){
    }
-not need to declare variable seperately and not need this.x=x and those stuf in constructor.

12)properties:
-get and set to get value and set value of class. which are private.

13)modules:
-import and export class as a module in typescript.